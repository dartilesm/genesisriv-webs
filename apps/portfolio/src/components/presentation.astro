---
import Heading from "@/components/ui/heading.astro";
import type { Portfolio } from "@/types/index";

type Props = {
  data: Portfolio["presentation"];
};

const { data } = Astro.props;
---

<section id='presentation' class='w-full pt-12 md:pt-24 lg:pt-32 xl:pt-48'>
  <div class='container px-4 md:px-6 mx-auto'>
    <div class='flex flex-col items-center space-y-4 text-center'>
      <img
        src='/profile.svg'
        alt='Profile Image'
        class='max-w-60 [aspect-ratio:176/215] rounded-full mx-auto'
      />
      {
        data?.title && (
          <Heading title={data?.title} description={data?.description} cta={data?.cta} />
        )
      }
    </div>
  </div>
</section>
